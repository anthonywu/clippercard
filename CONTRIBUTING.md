# How to Contribute

1. Fork the repo
1. Install dev dependencies: `just sync`
1. Make your changes
1. Format and lint: `just format` and `just lint`
1. Add unit tests; run `just test` (or `just test-all` across supported Pythons)
1. Send me a pull request w/ explanation of design decisions
