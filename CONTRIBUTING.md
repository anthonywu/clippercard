# How to Contribute

1. Fork the repo
1. Install dev dependencies: `uv sync`
1. Make your changes
1. Run `ruff check .` and `ruff format .` on your code
1. Add unit tests, make sure they pass: `pytest`
1. Send me a pull request w/ explanation of design decisions
