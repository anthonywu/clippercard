[build-system]
requires = ["uv_build>=0.12.5,<0.13"]
build-backend = "uv_build"

[project]
name = "clippercard"
version = "202608.19.1"
description = "Unofficial Python API for Clipper Card (transportation pass used in the SF Bay Area)"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "Your friendly neighborhood transit rider-hackers", email = "noreply@github.com" },
]
classifiers = [
    "Intended Audience :: Developers",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Python :: 3.15",
    "Programming Language :: Python :: 3 :: Only",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "beautifulsoup4>=4.15.0",
    "rich>=15.0.0",
    "requests>=2.34.2,<3.0",
    "urllib3>=2.7.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.3",
    "build>=1.5.0",
    "twine>=7.0.0",
    "nox>=2026.8.17",
]

[project.scripts]
clippercard = "clippercard.main:main"

[project.urls]
Homepage = "https://github.com/clippercard/clippercard-python"
Repository = "https://github.com/clippercard/clippercard-python"

[tool.uv]
package = true

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]

[tool.uv.build-backend]
module-root = ""
